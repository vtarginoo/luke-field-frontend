import ContentLocal from "../../components/ContentLocal";
import OSTable from "../../components/OSTable";

const SectionListaOS = () => {
  return (
    <ContentLocal pagetitle="Ordens de Serviço">
      <OSTable />
    </ContentLocal>
  );
};

export default SectionListaOS;
