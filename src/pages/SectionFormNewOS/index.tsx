import ContentLocal from "../../components/ContentLocal";
import FormNewOS from "../../components/FormNewOS";

const SectionFormNewOS = () => {
  return (
    <ContentLocal pagetitle="Nova OS">
      <FormNewOS />
    </ContentLocal>
  );
};

export default SectionFormNewOS;
